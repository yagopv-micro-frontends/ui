import styled from 'styled-components';
import {
  withSpacingProps,
  withAlignmentProps,
  withOverflowProps,
} from '../../lib';

export default styled.div`
  ${withSpacingProps}
  ${withAlignmentProps}
  ${withOverflowProps}
`;
